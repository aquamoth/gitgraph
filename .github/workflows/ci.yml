name: CI

# PRs are tested on their merge result. Pushes to main run too: they catch main breaking when two
# PRs pass separately but conflict once merged, and they seed the Rust cache that PR runs restore
# from (PR runs cannot read each other's caches, only main's).
on:
  push:
    branches: [main]
  pull_request:

# A new push to a PR cancels the older run of that PR that is still in progress.
concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Test (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: Format
        run: cargo fmt --all --check
      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: Test
        run: cargo test --workspace
      - name: Build release
        run: cargo build --release
      - uses: actions/upload-artifact@v4
        with:
          name: gitgraph-${{ matrix.os }}
          path: |
            target/release/gitgraph
            target/release/gitgraph.exe
          if-no-files-found: ignore
